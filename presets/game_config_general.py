"""General game configuration entry point."""

from .base import DefaultGameFlow


class GeneralGameConfig(DefaultGameFlow):
    """Default game flow used when no specialized preset is requested."""

    pass
